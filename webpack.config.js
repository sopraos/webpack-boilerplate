// Webpack configuration
// ===\===================

const path = require('path');

module.exports = env => {
	const isDevelopment = env.development === true;

	let config = {
	};

	return config;
};
